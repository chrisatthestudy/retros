@rem Batch file for setting up the paths and environment variables required
@rem for running the tools from the command-line.

@rem Set up NASM path
@set path=R:\apps\nasm;%path%

@rem Set up Qemu path
@set path=R:\apps\qemu;%path%
